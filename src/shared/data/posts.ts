import { CATEGORY as CATEGORY_ENUM, Post, STATUS_POST } from '~/types/post'

export const CATEGORY: CATEGORY_ENUM[] = [
  CATEGORY_ENUM.FRONT_END,
  CATEGORY_ENUM.BACK_END,
  CATEGORY_ENUM.FULL_STACK,
  CATEGORY_ENUM.MOBILE,
  CATEGORY_ENUM.GAME
]
